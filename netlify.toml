[build]
  # 빌드 명령어를 비워둡니다.
  command = ""
  # 은하수님의 파일이 모여있는 폴더를 지정합니다.
  publish = "public"

[functions]
  # 서버 함수(에어드랍 기능)가 들어갈 폴더입니다.
  directory = "netlify/functions"

[[redirects]]
  # 어떤 주소로 들어와도 index.html로 연결하여 404를 방지합니다.
  from = "/*"
  to = "/index.html"
  status = 200